#include <stdio.h>

int isPrime(int num) {
  
}

void main() {
  int num;

  printf("Insira um número para saber se ele é primo ou não: ");
  scanf("%d", &num);

  if (isPrime(num)) printf("O número %d é primo.", num);
  else printf("O número %d é primo.", num);
}